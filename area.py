# Written by Zakaria Ridouh
from math import sqrt, pi
'''Area Calculator'''


def square(side):
    print "Running the values through the solver!"
    return int(pow(side, 2))


def rectangle(height, length):
    print "Running the values through the solver!"
    return height*length


def circle(r):
    print "Running the values through the solver!"
    return pi*(pow(r, 2))
